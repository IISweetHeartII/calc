---
description: Run code quality checks (lint, format, typecheck)
---

Run the following commands to check code quality.

// turbo-all

1. Run lint
   pnpm run lint

2. Run format check
   pnpm run format:check

3. Run type check
   pnpm run typecheck
